import React from 'react';
import logo from '../img/logo.png';

function header() {
  return (
    <header className="center">
      <img src={logo} alt='breaking bad sybmol' />
    </header>
  );
}

export default header;
